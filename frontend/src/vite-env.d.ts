/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` from this package's own
// package.json version — see CLAUDE.md's "Versioning" section.
declare const __APP_VERSION__: string;
